@model IEnumerable<CRM_AGD.Models.StreetPrefix>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Prefix)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Prefix)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StreetPrefixId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StreetPrefixId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StreetPrefixId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
